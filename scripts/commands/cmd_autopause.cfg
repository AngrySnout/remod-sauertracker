autopause = [
    if (!=s $arg2 "") [
        autopause_on = $arg2
    ]
    say (concatword "^f1Autopause is now ^f0" (? $autopause_on "enabled" "disabled" ))
]

registercommand "autopause" autopause 2 "i" "autopause [0|1] - Pause the game when someone leaves"

